apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: aos
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: aos
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
